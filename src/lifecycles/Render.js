//render re renders the whole componnent whenever there is a change in state or props
//render life cycle is used to get the updated state or updated props value
//after constructor render is called

import React, { Component } from 'react'

export default class Render extends Component {
  render() {
    return (
      <div>Render</div>
    )
  }
}
